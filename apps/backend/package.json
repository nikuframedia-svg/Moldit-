{
  "name": "@prodplan/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run python -m src.main",
    "test": "uv run pytest",
    "lint": "uv run ruff check src/",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null; rm -rf .pytest_cache"
  }
}
