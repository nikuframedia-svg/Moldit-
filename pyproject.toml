[project]
name = "pp1-scheduler"
version = "1.0.0"
description = "ProdPlan PP1 — Industrial APS Scheduler"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
