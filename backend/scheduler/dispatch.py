"""Phase 3 — Dispatch: Moldit Planner.

Will be rewritten in Phase 3 with Moldit-specific dispatch logic.
"""

from __future__ import annotations


def assign_machines(*args, **kwargs):  # type: ignore[no-untyped-def]
    """Stub — Phase 3."""
    raise NotImplementedError("Moldit dispatch — Phase 3")


def sequence_per_machine(*args, **kwargs):  # type: ignore[no-untyped-def]
    """Stub — Phase 3."""
    raise NotImplementedError("Moldit sequence — Phase 3")


def per_machine_dispatch(*args, **kwargs):  # type: ignore[no-untyped-def]
    """Stub — Phase 3."""
    raise NotImplementedError("Moldit dispatch — Phase 3")
